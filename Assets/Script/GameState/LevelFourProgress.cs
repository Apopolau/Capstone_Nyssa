using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LevelFourProgress : MonoBehaviour
{
    bool hasFoundRabbit;

    bool securedWater;

    int treeCount;
    int grassCount;
    int flowerCount;

    

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
